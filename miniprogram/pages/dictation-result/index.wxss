/* 听写结果页面样式 */
.result-container {
  padding: 0;
  height: 100vh;
  /* 固定高度 */
  display: flex;
  flex-direction: column;
  background-color: #e8f5e9;
  position: relative;
  overflow: hidden;
  /* 防止整体滚动 */
  box-sizing: border-box;
  padding: 20rpx 0rpx;
}

/* 导航栏样式 */
.custom-nav {
  background-color: #4CAF50 !important;
  box-shadow: none !important;
  border-bottom: none !important;
}

.nav-title {
  color: #ffffff !important;
  font-weight: bold !important;
}

/* Vant导航栏返回按钮颜色 */
.van-nav-bar__text {
  color: #ffffff !important;
}

.van-icon-arrow-left {
  color: #ffffff !important;
}

/* 记录项 */
.record-item {
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin: 0 20rpx 20rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
  width: calc(100% - 40rpx);
  box-sizing: border-box;
}

.record-header {
  margin-bottom: 20rpx;
  padding-bottom: 20rpx;
  border-bottom: 2rpx solid #f5f5f5;
}

.record-info {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.record-title-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 20rpx;
}

.record-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.record-image {
  width: 80rpx;
  height: 80rpx;
  border-radius: 8rpx;
  overflow: hidden;
  flex-shrink: 0;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.1);
}

.record-image image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.record-time {
  font-size: 24rpx;
  color: #999;
}

/* 记录内容区域 */
.record-content {
  padding-top: 20rpx;
}

/* 记录统计 */
.record-stats {
  display: flex;
  justify-content: space-around;
  align-items: center;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  padding: 10rpx 20rpx;
  border-radius: 10rpx;
  transition: all 0.3s;
}

/* 可点击的统计项样式 */
.stat-item:nth-child(1), .stat-item:nth-child(2) {
  cursor: pointer;
  position: relative;
  overflow: hidden;
  min-width: 120rpx;
}

.stat-item:nth-child(1):active, .stat-item:nth-child(2):active {
  background-color: rgba(0, 0, 0, 0.05);
}

/* 选中状态的背景效果 */
.stat-item.active-all {
  background-color: rgba(76, 175, 80, 0.15);
  box-shadow: 0 2rpx 8rpx rgba(76, 175, 80, 0.2);
}

.stat-item.active-wrong {
  background-color: rgba(244, 67, 54, 0.15);
  box-shadow: 0 2rpx 8rpx rgba(244, 67, 54, 0.2);
}

.stat-value {
  font-size: 40rpx;
  font-weight: bold;
  color: #4CAF50;
  margin-bottom: 8rpx;
  transition: all 0.3s;
}

/* 错误数字样式 */
.stat-value.wrong {
  color: #f44336;
}

/* 激活状态样式 */
.stat-value.active {
  transform: scale(1.1);
  text-shadow: 0 0 8rpx rgba(76, 175, 80, 0.4);
}

.stat-value.wrong.active {
  text-shadow: 0 0 8rpx rgba(244, 67, 54, 0.4);
}

.stat-label {
  font-size: 24rpx;
  color: #666;
  transition: all 0.3s;
}

.stat-label.active {
  color: #333;
  font-weight: bold;
}

.correct {
  color: #4CAF50;
}

.wrong {
  color: #F44336;
}

/* 过滤提示文本 */
.filter-hint {
  font-size: 24rpx;
  color: #666;
  font-weight: normal;
}

/* 记录操作 */
.record-actions {
  display: flex;
  justify-content: flex-end;
}

.record-action {
  font-size: 28rpx;
  color: #2196F3;
  padding: 10rpx 20rpx;
}

/* 单词详情部分 */
.details-section {
  margin: 0;
  /* 移除左右边距 */
  margin-right: -2rpx;
  /* 确保完全贴合右侧 */
  background-color: #fff;
  border-radius: 20rpx 0 0 0;
  /* 只保留左上角圆角 */
  padding: 30rpx 0 30rpx 30rpx;
  /* 移除右侧内边距 */
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
  flex: 1;
  /* 填充剩余空间 */
  width: 100%;
  /* 宽度铺满 */
  overflow: hidden;
  /* 仅允许内部滚动 */
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

.section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 20rpx;
  flex-shrink: 0;
  /* 防止压缩 */
  padding-right: 30rpx;
  /* 为标题单独添加右侧内边距 */
}

.word-list {
  flex: 1;
  /* 占据details-section剩余空间 */
  overflow-y: scroll;
  /* 显式设置为scroll以确保显示滚动条 */
  -webkit-overflow-scrolling: touch;
  /* 增强iOS滚动体验 */
  padding-right: 30rpx;
  /* 为单词列表内容单独添加右侧内边距 */
  margin-right: 0;
  /* 确保滚动条贴近边缘 */
  box-sizing: border-box;
}

.word-item {
  padding: 15rpx 20rpx;
  margin-bottom: 10rpx;
  border-radius: 8rpx;
  background-color: #f9f9f9;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
}

.word-item.correct {
  background-color: rgba(76, 175, 80, 0.1);
}

.word-item.wrong {
  background-color: rgba(244, 67, 54, 0.1);
}

.word-index {
  font-size: 28rpx;
  font-weight: bold;
  color: #666;
  margin-right: 10rpx;
  min-width: 40rpx;
}

.word-text {
  font-size: 32rpx;
  font-weight: bold;
  flex: 1;
}

.word-answer {
  width: 100%;
  margin-top: 10rpx;
  margin-left: 50rpx;
  font-size: 28rpx;
  color: #F44336;
}